# A2UI 销售查询示例实现计划

使用 writing-plans 制定计划，在当前会话顺序实现。用户已确认，无需重复审批。

**目标：** 可运行的前后端分层销售查询示例。
**架构：** SQLite → SalesService → A2UI NDJSON → SurfaceStore → DOM 渲染；action 回传。
**技术栈：** Python 标准库、SQLite、JavaScript modules。

## 步骤
- [x] 编写 tests/test_a2ui_sales.py，运行虚拟环境 unittest，验证实现缺失而失败。
- [x] 实现 data.py 的 SalesRepository.snapshot(period, region)、service.py 的 SalesService.query(text)/action(message)、protocol.py 消息构建。
- [x] 实现 server.py 的 create_server(host, port)，限制静态路径、请求大小，返回中文错误。
- [x] 编写 tests/a2ui_renderer.test.mjs，验证缺少前端状态实现；实现 web/state.mjs、renderer.mjs、app.mjs、catalog.json、HTML/CSS。
- [x] 运行后端、前端与真实 HTTP 验证；浏览器无可用连接，视觉/实际点击未验收。
- [x] 更新示例 README 与根索引，执行 git diff --check，记录验收边界。

## 约束
使用现有 .venv/Scripts/python.exe，无全局安装。金额以整数分存储，参数化 SQL，仅支持确定的月份、区域和查询意图。不支持的输入返回明确错误。自定义表格不能宣称官方标准组件。


最终结果：11 项 Python 测试（含 2 项官方 Schema）和 7 项 Node 状态/DOM 测试通过。git diff --check 通过。未新增运行依赖；浏览器验收受环境限制。
