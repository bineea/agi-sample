# A2UI 真实 Agent 实现计划

按用户已确认链路执行，使用 writing-plans/TDD，当前会话顺序实施。

- [x] 新增 tests/test_a2ui_agent.py：SDK 模拟 HTTP、受限工具、模型自定义布局、追问/点击上下文、会话与坏输出边界；先验证缺少实现失败。
- [x] 提取 protocol.build_data；新增 llm.py 配置/SDK适配、agent_tools.py 工具、agent_ui.py Schema/图/绑定校验、agent.py 工具循环/会话。
- [x] server.py 默认 agent、显式 demo、会话头、元数据/中文错误；前端加多轮聊天、新会话、实际工具记录，取消自动模型调用。
- [x] 更新 requirements-agent.txt、.env.example 和 README；迁移官方 Schema 至运行包以便运行时校验。
- [x] 运行全部 A2UI Python/Node 测试、语法与 diff 检查。无有效模型配置或浏览器时如实说明未验收。


验证结果：27 项 Python 测试、8 项 Node 测试通过；Python 编译、前端模块语法及 git diff --check 通过。真实模型凭据未配置，浏览器没有连接；远端端到端和视觉验收未完成。
